/* const total = 50; // global scope;

const sum = (firstNum, secondNum) =>{
    const sumIs = firstNum + secondNum; // function scope
    console.log(sumIs);
    if(firstNum>secondNum){
        const sub = firstNum * secondNum; //Block scope
        console.log(sub)
    }
    else{
        const mul = firstNum / secondNum;//Block scope
        console.log(mul)
        
        const income = total;
        console.log(income);
    }

}

const output = sum(10,20);


 */